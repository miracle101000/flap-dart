[package]
name = "flap-emit-dart"
version = "0.1.0"
edition = "2024"

[dependencies]
flap-ir = { path = "../flap-ir" }
minijinja = { version = "2", features = ["loader"] }
serde     = { version = "1", features = ["derive"] }