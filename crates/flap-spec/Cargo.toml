[package]
name = "flap-spec"
version = "0.1.0"
edition = "2024"

[dependencies]
flap-ir  = { path = "../flap-ir" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1"
anyhow = "1"
ureq = { version = "2", features = ["tls"] }
