/// The version of this package.
///
/// Must match `version:` in `pubspec.yaml` — it selects which GitHub release
/// the native binary is downloaded from (`v<packageVersion>`). A test in
/// `test/version_test.dart` enforces the match.
const String packageVersion = '0.0.5';
